# Prompt لإعطائه لـ AI ثاني (مثل ChatGPT / Claude / Gemini) لتصليح واجهة Qjo

---

## سياق المشروع

أنا عندي مشروع دردشة ذكاء اصطناعي اسمه **Qjo** شغال على Render كـ production، والباك إند والـ auth (Firebase) وكل الوظائف (إرسال رسائل، ملفات، توليد كود، بحث، تفكير، محادثات محفوظة) شغّالة 100% ومختبرة 34/34 smoke tests. المشكلة فقط في **الواجهة (CSS/front-end styling)**.

المشروع مكتوب بـ:
- **Backend**: Node.js + Express (`server.js`)
- **Frontend**: vanilla HTML/CSS/JS (لا React/لا frameworks)
  - `public/index.html` — الهيكل
  - `public/app.js` — منطق الواجهة
  - `public/styles.css` — الستايل الأصلي
  - `public/design-system.css` — تصميم نظام أساسي
  - `public/premium-ui.css` — ملف الستايلات الجديدة الفاخر (**هذا الملف سبّب كل المشاكل**)

## الهدف المطلوب

1. **إعادة تصميم الواجهة بالكامل** لتطابق مرجع بصري اسمه "Ruixen AI":
   - خلفية سوداء عميقة (#05030d) مع هالة **aurora** ضخمة متوهجة بالبنفسجي والأزرق والنيون **خلف صندوق الكتابة مباشرة** (نصف دائرة مضيئة)
   - خلفية توجد فيها **نقاط/دوت جريد dot grid** خافتة أنيقة
   - عنوان ضخم في المنتصف: **"ابنِ شيئًا مذهلاً"** (كلمة "مذهلاً" بتدرج بنفسجي-وردي)
   - نص فرعي تحت العنوان (عربي RTL)
   - **صندوق كتابة زجاجي (glassmorphism)** كبير وسطي، max-width ~820px، بزوايا دائرية واسعة، مع:
     - زر إرفاق (مشبك) يسارًا
     - زر إرسال (سهم) يمينًا بتصميم متدرج بنفسجي-وردي
   - صف أزرار **pill chips** أنيقة تحت الكومبوزر مع أيقونات SVG: توليد كود، إطلاق تطبيق، مكونات UI، ثيمات، لوحة مستخدم، صفحة هبوط، رفع مستندات، صور وأصول
   - حبوب/أزرار وظيفية صغيرة داخل الكومبوزر (بحث، تفكير، وضع الطاقة) بتصميم pill أنيق
   - **لا سايد بار قاسية/محددة بخط حاد** — السايدبار الحالية تبقى موجودة لكن بتصميم زجاجي داكن متناسق مع الثيم
   - اتجاه **RTL عربي محفوظ** بالكامل (النص والأيقونات والسهم)
   - **دعم وضع الدارك فقط** (لا حاجة light mode لأنه كان بيعمل مشاكل)
   - تأثيرات hover ناعمة (رفع/دوران خفيف) على كل الأزرار
   - شريط علوي (topbar) أنيق زجاجي

## الشروط الحاسمة (اقرأها جيدًا — هذا اللي خرب الدنيا في المحاولات السابقة)

1. **لا تقرّب أبدًا من `public/app.js` ولا من `server.js`** — الباك إند والمنطق شغالين 100%، أي تعديل عليهم بيخرب الدنيا. فقط تعديل ملفات الستايل، ويمكن تعديل `index.html` بشكل محدود جدًا لإضافة عناصر/أيقونات شريطة ألا تحذف أو تغيّر أي `id` موجود أو أي بنية existing elements.

2. **لا تضف أي `pointer-events: none` على عناصر شائعة** (body, .app, .main, .composer-wrap, .composer-shell, .topbar, .messages, etc.) — استخدامها كان سبب رئيسي في كسر النقر.

3. **لا تحط `display: grid/flex/none` افتراضيًا على `.modal-backdrop` أو أي overlay class** — يفترض يكون `display: none` افتراضيًا ويعرض فقط عند إضافة الكلاس `.show`. استخدام `display:grid` كافتراضي غطّى كامل الشاشة بطبقة شفافة وخلّى كل الأزرار غير قابلة للنقر.

4. **لا تستخدم `z-index` أعلى من z-index الـ auth overlay (220)** للعناصر العادية، لأنها رح تظهر فوق شاشة تسجيل الدخول. الكومبوزر والـ chips يكفيهم z-index 2-5.

5. **لا تغيّر `overflow`, `position`, `display` على `.app`, `.main`, `.messages`** بطريقة تكسر التدفق الطبيعي للصفحة — فقط استخدم `background`, `color`, `box-shadow`, `border`, `padding`, `border-radius`, `backdrop-filter` للتجميل.

6. **احترم الـ RTL بالكامل**:
   - أي سهم أو أيقونة اتجاهية (مثل زر الإرسال) تنعكس بـ `transform: rotate(180deg)` في RTL
   - استخدم `border-inline-end` بدل `border-right`، و`margin-inline`/`padding-inline` بدل left/right
   - لا تحط اتجاه LTR hardcoded

7. **ملف `premium-ui.css` يجب أن يكون آمنًا**:
   - كل selector يخص الـ styling فقط (مثلاً `.composer-shell`, `.send-btn`, `.sidebar`, الخ)
   - لا تُعدّل الـ layout الأصلي للكومبوزر (هو display: grid عند الأصلي، لا تغيّره لشيء آخر يكسّر مكان textarea مقابل زر الإرسال)
   - لا تضع pseudo-elements `::before`/`::after` على الكومبوزر تمتد خارج حدودها وتغطي أزرار أخرى بدون `pointer-events: none`
   - `backdrop-filter` ممكن استخدامه على العناصر الزجاجية فقط، وعلى Safari استخدم `-webkit-backdrop-filter`

8. **احترام الـ dark mode الأصلي**:
   - التطبيق عنده class `.dark` على `<body>` للوضع الداكن
   - اجعل الثيم الداكن هو الافتراضي ويعمل حتى لو لم يكن `body.dark` موجود، عبر كتابة الستايلات الأساسية وعدم الاعتماد على وجود body.dark فقط

9. **ضع في اعتبارك الموبايل**:
   - Media query للشاشات الصغيرة (<820px)
   - الكومبوزر يأخذ عرض الشاشة
   - الأزرار تصغر
   - الـ sidebar تصير drawer مثل ما هي الآن (لا تحتاج تعديلها على الموبايل)

10. **Cache busting**: عند إنهاء الستايل، غيّر query string الـ version على روابط CSS في index.html (مثل `premium-ui.css?v=qjo-aurora-final`) حتى المتصفح يعمل hard reload.

11. **لا تضف أي JavaScript جديد** — الـ JS الموجود بالفعل يتعامل مع كل الـ event listeners، الأفاتار، الـ toggles، الخ.

12. **تأكد أن كل العناصر التالية ظاهرة وقابلة للنقر بعد التعديل**:
    - زر "محادثة جديدة" في السايدبار
    - زر الإرسال (زر السهم البنفسجي)
    - زر الإرفاق (مشبك)
    - زر المايك
    - أزرار الفئات السريعة (chips) تحت الكومبوزر
    - زر تسجيل الخروج في بطاقة الحساب
    - زر الإعدادات (الترس)
    - زر إظهار/إخفاء السايدبار في التوب بار
    - كبسة "بحث" و"تفكير" داخل الكومبوزر
    - روح/نزول لآخر محادثة
    - صندوق الكتابة (textarea) قابل للكتابة
    - كل الـ modals (الإعدادات، كل المحادثات، تدريب النص، اختيار الأفاتار) تفتح وتغلق بشكل طبيعي

## طريقة العمل الآمنة

1. **اقرأ الملفات الحالية أولًا**: `public/index.html`, `public/styles.css`, `public/design-system.css`, `public/app.js` (لفهم الـ classes والـ IDs الموجودة فقط، لا تعدّله).
2. **اكتب ملف `public/premium-ui.css` من الصفر** كـ layer إضافي فوق الستايلات الأصلية، لا يحذف ولا يستبدل الستايل الأصلي بل يضيف فوقه (cascade).
3. **اختبر منطقيًا**: أي rule بتكتبها اسأل حالك "هل هذه الـ rule ممكن تغطي عنصر آخر؟" — إذا كان الجواب نعم، أضف `pointer-events: none` على pseudo-elements، أو ضمّن الـ selector بشكل أضيق.
4. **لا تلمس `app.js` و`server.js` أبدًا**.
5. **في `index.html`**: مسموح لك فقط بـ:
   - تحديث cache-bust query على روابط CSS/JS
   - إضافة SVG icons داخل الأزرار الموجودة (لا تغيّر الـ IDs)
   - لا تحذف عناصر موجودة
6. أعطني الملفات النهائية كاملة (premium-ui.css، وأي تعديل طفيف على index.html فقط إن لزم) مع شرح مختصر لكل جزء.

## ملاحظات إضافية عن التصميم المطلوب (Ruixen AI style)

- **اللوغو/البراند**: "Qjo" بتصميم حديث، "Assistant" تحته بحروف صغيرة
- **فقاعات المستخدم**: بتدرج بنفسجي-أزرق، نص أبيض
- **فقاعات المساعد**: زجاجية بخلفية شفافة داكنة وحد خفيف
- **ألوان أساسية**:
  - الخلفية: `#05030d` (داكن جداً) / `#0a0818` (أفتح شوي)
  - البنفسجي: `#8b5cf6`, `#a855f7`
  - الوردي: `#ec4899`
  - الأزرق: `#3b82f6`, `#22d3ee`
  - النص الرئيسي: `#f1f5f9`
  - النص الثانوي: `#94a3b8`
- **الحدود الزجاجية**: `rgba(255,255,255,0.08)` إلى `rgba(255,255,255,0.18)`
- **الـ glow عند التركيز على الكومبوزر**: حلقة توهج ملونة متعددة الألوان (تدرج دائري)، وليست outline عادي
- **الـ aurora**: blob ضخم متحرك ببطء خلف الكومبوزر يعطي إحساح "كوكب مضيء"
- **الـ typography**: sans-serif حديث، وزن ثقيل 800 للعناوين، تباعد حروف -0.04em للعنوان الرئيسي
- **RTL**: كل النص العربي يظهر من اليمين لليسار، الأيقونات direction-aware
- **الـ welcome section**: لا يوجد سوى h1 + p، لا شعارات ولا أزرار إضافية ولا kicker "Qjo · مساعدك الذكي" (كان موجود في نسخة سابقة، لا نريده الآن)

## أهم بند: لا تعيد اختراع العجلة

استخدم فقط **CSS selectors** على classes/IDs الموجودة بالفعل في HTML الأصلي. لا تُضف divs wrapper جديدة، لا تُغيّر البنية، لا تُضف JavaScript، لا تُبدّل أي element positions من `relative/absolute` إلى شيء آخر ما لم يكن آمنًا. الهدف هو "skinning" للواجهة الحالية، ليس إعادة بنائها.

---

## لقطة شاشة مرجعية

الصورة المرجعية للتصميم المطلوب موجودة في هذا المسار في المشروع:
`/home/user/uploads/Screenshot 2026-09-02 224100.png`

افتحها وانظر إلى شكل الخلفية والأورورا والكومبوزر الزجاجي والأزرار — المطلوب تقليد هذا الأسلوب على Qjo.

---

## اختبارات بعد الانتهاء

بعد كتابة CSS، أكّد أن:
1. الصفحة تحمل بدون أخطاء كونسول (افتح DevTools)
2. كل زر قابل للنقر
3. الكتابة في textarea تعمل
4. إرسال رسالة يعمل
5. فتح/إغلاق السايدبار يعمل
6. تسجيل الدخول والخروج يعمل (لا شاشة فاضية)
7. الـ modals تفتح وتغلق
8. الـ RTL مضبوط
9. التصميم يطابق المرجع (خلفية داكنة، aurora، glass composer، pill chips)
10. لا يوجد أي overlay شفاف يغطي الشاشة
